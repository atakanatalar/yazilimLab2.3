import { ArastirmaciIsimPipe } from './arastirmaci-isim.pipe';

describe('ArastirmaciIsimPipe', () => {
  it('create an instance', () => {
    const pipe = new ArastirmaciIsimPipe();
    expect(pipe).toBeTruthy();
  });
});
