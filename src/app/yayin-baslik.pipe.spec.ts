import { YayinBaslikPipe } from './yayin-baslik.pipe';

describe('YayinBaslikPipe', () => {
  it('create an instance', () => {
    const pipe = new YayinBaslikPipe();
    expect(pipe).toBeTruthy();
  });
});
